# Browser build: /api is portable when the web app and PHP API share a domain.
# For a separate API host or native app build, use https://api.your-domain.example.
VITE_API_URL=/api
VITE_RING_PURCHASE_URL=

# PHP runtime configuration.
# Use dev only locally to include exception details in 500 responses.
DEBUG=prod

# Relative database paths are resolved from the repository root.
BACKONTRACK_DB_PATH=private/data.db

# Generated cleanup backups are stored outside private/ and rotated automatically.
BACKONTRACK_BACKUP_DIR=backups
BACKONTRACK_BACKUP_KEEP=3

# Generate with: php -r 'echo bin2hex(random_bytes(32)), PHP_EOL;'
BACKONTRACK_API_SECRET=CHANGE_ME

# Authorizes the deployment workflow to call GET /server/migrate.php.
# Generate a separate value using the same command as BACKONTRACK_API_SECRET.
BACKONTRACK_MIGRATION_KEY=

# Same-origin browser requests are always allowed. Add only exact extra origins.
# Include http://localhost for Android and capacitor://localhost for iOS builds.
BACKONTRACK_ALLOWED_ORIGINS=https://your-domain.example,https://admin.your-domain.example,http://localhost:5183,http://127.0.0.1:5183,http://localhost:5184,http://127.0.0.1:5184,http://localhost,capacitor://localhost

# Seven days. Allowed range: 300 to 2592000 seconds.
BACKONTRACK_TOKEN_TTL=604800

# Supports interval definitions up to 2 MB plus request overhead.
BACKONTRACK_MAX_BODY_BYTES=2500000

# Public browser URL used in email confirmation and password reset links.
BACKONTRACK_APP_URL=https://your-domain.example

# Separate static administrator dashboard and its shorter privileged-session lifetime.
BACKONTRACK_ADMIN_URL=https://admin.your-domain.example
BACKONTRACK_ADMIN_TOKEN_TTL=28800

# SMTP delivery used by PHPMailer. Use tls with port 587 or ssl with port 465.
# Set encryption to none only for a trusted local SMTP development server.
BACKONTRACK_MAIL_HOST=smtp.example.com
BACKONTRACK_MAIL_PORT=587
BACKONTRACK_MAIL_USERNAME=
BACKONTRACK_MAIL_PASSWORD=
BACKONTRACK_MAIL_ENCRYPTION=tls
BACKONTRACK_MAIL_FROM_ADDRESS=backontrack@your-domain.example
BACKONTRACK_MAIL_FROM_NAME=BackOnTrack

# Optional AI assistant. The API key is used only by the PHP server.
BACKONTRACK_OPENAI_API_KEY=
BACKONTRACK_OPENAI_BASE_URL=https://api.openai.com/v1
BACKONTRACK_OPENAI_MODEL=gpt-5.6-terra
BACKONTRACK_OPENAI_DAILY_TOKEN_LIMIT=30000

# Android-only passkeys. RP ID must be the host serving /.well-known/assetlinks.json.
# Key hashes are unpadded base64url SHA-256 hashes of allowed Android signing certificates.
# Leave all three values empty to disable passkeys.
BACKONTRACK_PASSKEY_RP_ID=
BACKONTRACK_PASSKEY_ANDROID_PACKAGE=
BACKONTRACK_PASSKEY_ANDROID_KEY_HASHES=
